/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/react" />

/** Injected by vite.config.ts at build time — see src/app/build.ts. */
declare const __APP_BUILD__: { version: string; commit: string; date: string }
